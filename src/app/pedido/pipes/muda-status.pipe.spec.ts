import { MudaStatusPipe } from './muda-status.pipe';

describe('MudaStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MudaStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
