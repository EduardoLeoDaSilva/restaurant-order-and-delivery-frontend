import { InputDataFormatDirective } from './input-data-format.directive';

describe('InputDataFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDataFormatDirective();
    expect(directive).toBeTruthy();
  });
});
